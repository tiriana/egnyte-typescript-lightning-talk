Plan:

* plain function
* typed function
* class
    * show getters (avaiable only for es5+)
* interface
    * implements
    * extends !!! :-)
* module patterns
* importing external libs (d.ts files)
